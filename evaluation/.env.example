# Evaluation framework uses API keys from backend/.env
# Make sure backend/.env has GEMINI_API_KEY set (used as eval judge + response LLM)
#
# To run evaluation:
#   cd evaluation && uv run python run_all.py
#
# The evaluation does NOT require the server to be running.
# It loads keys directly from backend/.env via dotenv.
